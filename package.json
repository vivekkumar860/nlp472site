{
  "name": "nlp-hub",
  "version": "1.0.0",
  "private": true,
  "description": "NLP Curriculum Dashboard & Interactive Learning Hub",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=nlp-hub"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
